<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.neuedu.model.mapper.LWRecordMapper" >  

   <insert id="addLWRecord" parameterType="LWRecord">
      insert into lwrecord values(null,#{openid},#{cid},#{lwid},#{lwcount},#{aid},#{stime},#{price})
   </insert>
       
</mapper>